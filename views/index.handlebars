<div class = "container">
    <div class = "row">
        <div class = "col-md-4"></div>
        <div class = "col-4">
            <h1>Eat-Da-Burger!</h1>
        </div>
        <div class = "col-md-4"></div>
        <br><br><br>
    </div>

    <div class = "row">
            <div class = "col-md-4"></div>
             <div class = "col-md-4">
                 <h5>What Type of Burger Would You Like to Make?</h5>
                 <img id="burger" src="../assets/images/burger.jpg" height="200px" width="250px">
                 <br><br>
                 <form>
                     <div class="form-group">
                         <input type="text" class="form-control" id="new-burger" placeholder="Enter a Burger Here!" >
                     </div>
                     <button class="btn btn-primary" id = "add-burger">Add Burger</button>
                 </form>
             </div>
             <div class = "col-md-4"></div>             
         </div><br>


    <div class = "row menu">
        <div class = "col-md-4">
            <h2>Can't Wait to Eat These Burgers! </h2>
            <ul class = "list-group">
                {{#each burger}}
                    {{#unless devoured}}
                    <li class = "list-group-item" id = "{{id}}">{{burger_name}} <button data-value ="{{id}}" class = "devour btn btn-primary">Eat Me!</button></li>
                    {{/unless}}
                {{/each}}
            </ul>
        </div>
         <div class = "col-md-4"></div>
        <div class = "col-4">
            <h2>Yum - Already Ate These! </h2>
            <ul class = "list-group">
                {{#each burger}}
                    {{#if beenEaten}}
                    <li class = "list-group-item" id = "{{id}}">{{burger_name}} 
                        {{#if devoured}}
                        <button data-value ="{{id}}" class = "revive btn btn-primary">Too Good! Make Again!</button>
                        {{/if}}
                        <div class = "row">
                            <small>Burgers Eaten: {{eaten}}</small>
                        </div>
                    </li>
                    {{/if}}
                {{/each}}
            </ul>
        </div>
    </div>


    
</div>
